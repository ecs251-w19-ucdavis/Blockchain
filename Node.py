import os
import datetime
import transactions

class node:
    neigthbor = []
    address = ''
    def mining(transactions):
        
    def transfer(to_address, amount):
        trans = transaction(self.address, to_address, amount)
        return trans
    def signTransactions():
        return